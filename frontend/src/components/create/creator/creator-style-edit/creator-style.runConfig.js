/*
import background from './styleTemplates/background.html'
import colors from './styleTemplates/colors.html';
import fonts from './styleTemplates/fonts.html';
import image from './styleTemplates/image.html';
import sections from './styleTemplates/sections.html';



CreatorDisplayRun.$inject = ['$templateCache'];

export default function CreatorDisplayRun($templateCache) {

    let sectionsArray = [  //todo reduce number of section constant collections to a couple
        {name: 'background', template: background },
        {name: 'colors', template: colors},
        {name: 'fonts', template: fonts},
        {name: 'image', template: image},
        {name: 'sections', template: sections}];

    for(let i=0; i<sectionsArray.length; i++){
        $templateCache.put(sectionsArray[i].name + '.html', sectionsArray[i].template);
    }


}

    */